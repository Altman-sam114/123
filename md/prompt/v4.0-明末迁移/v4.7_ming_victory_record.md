# v4.7 明末胜负链首片记录

日期：2026-07-05

本记录对应明末迁移 v4.7 的胜负链首片。目标是把 `崇祯十五年：天下裂变` 的主要天下局势目标先接入 `VictoryRules`，让默认明末剧本不再只沿用阿登 / 巴斯托涅胜负口径。

## 1. 本轮目标

- 保留 legacy 阿登胜负规则，不破坏旧测试板和旧数据 fallback。
- 对 `chongzhen_1642` 剧本启用明末胜负规则：
  - 清军控制山海关和北京，判定后金/清胜。
  - 大顺控制开封、洛阳和西安，判定大顺胜。
  - 大西控制荆州和武昌，判定大西胜。
  - 明廷在最终回合守住北京、山海关和武昌，判定明廷胜。
  - 最终回合未触发明确条件时，按 objective points 计算关键目标归属。
- 让顶部朝报令条的胜负 badge 显示简短胜负理由。
- 保持所有判定只读取 `GameState`、`MapState` 和 objective 控制权，不从 UI 或 Agent 直接改权威状态。

## 2. 已完成改动

- `Objective` 新增 `points`：
  - `DataLoader` 从 `ScenarioDefinition.objectives[].points` 写入 `MapState.objectives`。
  - `ScenarioDefinition.ObjectiveDefinition` 和 `Objective` 自定义 decode，旧剧本、旧存档或旧测试数据缺少 `points` 时默认 0。
- `MapState` 新增按 objective id 查询：
  - `objective(id:)`
  - `controllerOfObjective(id:)`
- `VictoryReason` 新增明末原因和 `displayName`：
  - `mingHoldsMandateAtFinalTurn`
  - `qingBreaksPassAndCapital`
  - `dashunControlsCentralPlain`
  - `daxiControlsHuguangBase`
  - `mingHoldsMostKeyObjectivesAtFinalTurn`
  - `mingMandateCollapsedAtFinalTurn`
- `VictoryRules` 分流：
  - `scenarioId.hasPrefix("chongzhen_1642")` 时走明末胜负链。
  - 其他剧本继续走 legacy 阿登胜负链。
- `HUDView` 胜负 badge 从只显示“某势力胜”升级为“某势力胜 · 胜负理由”。
- `RuleEngineCoreTests` 增加明末胜负规则语义用例，覆盖清、大顺、大西和明廷最终守线四类首片条件。

## 3. 非目标

- 不新增历史事件系统、教程触发器、灾荒事件或多回合战役目标链。
- 不新增完整政策/科技树，也不让政策直接改胜负。
- 不改变 `Command`、`ZoneDirective`、`WarCommandExecutor`、`RuleEngine` 权威链路。
- 不删除或重写 legacy 阿登胜负规则。
- 不声称完成发布级胜负体验；事件提示、教程解释、战役目标 UI 和截图验收仍后续继续。

## 4. 关键文件

- `WWIIHexV0/Core/MapState.swift`
- `WWIIHexV0/Core/VictoryState.swift`
- `WWIIHexV0/Data/ScenarioDefinition.swift`
- `WWIIHexV0/Data/DataLoader.swift`
- `WWIIHexV0/Rules/VictoryRules.swift`
- `WWIIHexV0/UI/HUDView.swift`
- `WWIIHexV0/Tests/RuleEngineCoreTests.swift`

## 5. 后续事项

- 将 `ScenarioDefinition.victoryConditions` 从 data schema 进一步编译为可数据驱动的胜负规则，减少硬编码 objective id。
- 给天下面板或朝廷面板增加战役目标列表，让玩家可见当前距胜负条件还差哪些目标。
- 在 v4.7 后续片加入松锦余波、催饷、饥荒、开封围城、湖广粮道等事件和 10-20 回合目标链。
- 在授权视觉验收时确认 HUD 胜负 badge 在 iPhone / macOS 窄宽度下不溢出。
