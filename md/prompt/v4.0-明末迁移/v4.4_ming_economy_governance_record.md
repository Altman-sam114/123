# v4.4 明末经济、地方治理与天下局势首片记录

## 1. 本轮目标

依据 `codex-v4.0-明末aiagent迁移总提示词.md` 的 v4.4 路线，本轮只落地明末经济、地方治理和天下局势首片：

- 把既有 v0.8 经济资源展示迁为民力、银两、粮草。
- 把生产 UI 和日志迁为募营兵、募精骑、募哨骑、造炮队、筹粮。
- 让明末势力完成生产后生成明末组件单位，而不是 legacy 装甲/摩托化单位。
- 让 AI 摘要能看到库存、收入、军粮维护、补员消耗和关键钱粮缺口。
- 修复默认明廷玩家在 `.humanAction` 阶段的 UI 可操作判断。
- 让 `OccupationState.resistance/compliance` 以民变/行政掌控口径影响州府钱粮产出。
- 把玩家信息面板中的外交入口改为“天下局势”，展示中华世界内诸方势力、战和关系、主要对手和朝议/军议。
- 将后续政策、经济、科技、军事四线并重和发布级地图/部队/朝廷 UI 要求写回项目大纲。

## 2. 非目标

- 不新增独立灾荒、瘟疫、民心、腐败、拖欠军饷、将领忠诚或士气字段。
- 不新增修城、赈济、征饷、屯田、整训团练或科技/军械项目等新生产/政策命令。
- 不改 hex / region / theater / front / deploy 权威边界。
- 不绕过 `Command.queueProduction -> RuleEngine -> EconomyRules` 的统一生产入口。
- 不删除 legacy Germany / Allies 和阿登 fallback。

## 3. 实现摘要

### 3.1 资源与生产显示

- `EconomyResources` 新增 `displaySummary` 和 `compactDisplaySummary`，统一输出民力、银两、粮草。
- `CityLevel.displayName` 改为无城镇、县镇、府州、京师/重镇。
- `ProductionKind.displayName` 改为募营兵、募精骑、募哨骑、造炮队、筹粮。
- `ProductionKind.producedUnitBaseName` 用于生成明末生产单位名。

### 3.2 经济规则

- `EconomyRules` 的 bootstrap、排产、结算、粮草短缺、自动补员、生产完成日志改为明末钱粮口径。
- `OccupationState.resistance/compliance` 以民变/行政掌控口径生成 `economicYieldPercent`，用于轻量修正州府钱粮产出。
- `EconomyRules.incomeContribution(for:faction:map:)` 统一计算单个州府产出，收入结算与州府面板共用，避免 UI 和规则口径分叉。
- 明末势力完成生产后生成明末组件单位：
  - 募营兵：步军、火器、骑兵。
  - 募精骑：旗骑、骑兵、火器。
  - 募哨骑：骑兵、步军、团练。
  - 造炮队：炮队、攻城器械、步军。
- legacy Germany / Allies 仍使用 `.infantry/.panzer/.motorized/.artillery` 工厂方法，保持旧数据兼容。

### 3.3 UI 与玩家操作阶段

- `GamePhase.displayName` 的通用阶段改为 AI 行动、玩家行动、结算；legacy 阶段显示为兼容中文。
- `GamePhase.allowsHumanCommands` 作为当前 App/UI 的可操作阶段判断，兼容 `.humanAction` 与 legacy `.alliedPlayer`。
- `AppContainer.bootstrap()` 默认把 `playerFaction` 设为明末剧本的第一个 `humanControlledFactions`，没有配置时才回退 `.allies`。
- `CommandPanelView`、`EconomyPanelView`、`HUDView`、`RegionInspectorView`、`UnitInspectorView`、`UnitTooltipView`、`EventLogView`、`RootGameView`、`WWIIHexV0MacApp` 做了钱粮和明末 UI 文案首片迁移。
- `RegionInspectorView` 增加治理摘要和钱粮修正展示；目标状态与地块标签继续清理玩家可见英文残留。
- `RootGameView` 的信息面板将外交 tab 改为“天下”。
- `DiplomacyPanelView` 改为“天下局势”面板，显示当前势力、名义主体、战事态势、主要对手、诸方势力、战和关系和朝议/军议。
- `DiplomaticStatus` 与 `RulerStrategicPosture` 的玩家可见显示名改为明末中文口径。

### 3.4 AI 摘要

- 新增 `EconomyAISummary`，包含库存、上回合收入、军粮维护、补员消耗和民力/银两/粮草缺口。
- 新增 `GovernanceAISummary`，统计控制州府数、不稳州府数、平均民变、平均行政和最低行政州府。
- `AgentContext`、`AgentPromptBuilder` 和 `TurnManager.contextSummary` 纳入钱粮摘要。
- `MarshalBattlefieldSummary` 纳入 `economySummary`，首片 schemaVersion 升到 6；治理摘要接入后 schemaVersion 升到 7。
- `SimulatedMarshalLLMClient` 的 summary 和 strategicIntent 会带上钱粮摘要。

## 4. 架构边界

- 经济仍是 faction 级总账，不是地图占领权威。
- 收入仍要求 region 有真实己方控制 hex 证据，不能把中立/空控制 region 收入算给某势力。
- 治理修正只影响经济收入和摘要展示，不改变 `HexTile.controller`、`RegionNode.controller`、`hexToTheater` 或 `hexToFrontZone`。
- 生产仍由 `Command.queueProduction` 进入 `RuleEngine` 校验和执行。
- 天下局势面板只读 `DiplomacyState` / `RulerDecisionRecord`，不直接改变关系、势力、朝议或战术状态。
- AI 只能读取钱粮摘要或输出结构化 directive，不能直接改 `economyState`、`HexTile.controller` 或 `Division.coord`。

## 5. 已知后续

- 补齐灾荒、民心、腐败和拖欠军饷对收入、补员、士气、将领满意度的影响。
- 增加修城、赈济、征饷、屯田、整训团练等生产/政策命令时，仍必须走统一命令入口。
- v4.5 需要把政策、经济、科技、军事四条线纳入朝议/军议 Agent directive；科技以火器、炮队、城防、农政、水利、驿道、屯田、漕运和训练法度表达，不做泛化 4X 科技树。
- 继续清理底层命令记录和 legacy 调试文案中的英文残留。
- 发布级 UI 仍需在 v4.6 对地图、部队、朝廷、天下、钱粮和战报做完整视觉与交互收口。

## 6. 本机轻量检查

- `git diff --check`：通过。
- `rg -n "[[:blank:]]+$" AGENTS.md README.md update_log.md md/test/test.md md/flow/flow.md md/flow/flowchart.md md/prompt/README.md md/plan/plan.md md/prompt/v4.0-明末迁移`：无命中。
- `rg -n "^(<<<<<<<|=======|>>>>>>>)" AGENTS.md README.md update_log.md md ... 本轮改动 Swift 文件`：无命中。
- `rg -n "[默]认先跑|默认 P[r]obe|P[r]obe -> Smoke|[S]tage Regression -> Full|代码改动按 .*P[r]obe|本机[默]认.*xcodebuild" AGENTS.md md/test/test.md md/flow/flow.md README.md`：无命中。
- `swiftc -parse WWIIHexV0/Core/Region.swift WWIIHexV0/Core/DiplomacyState.swift WWIIHexV0/Rules/EconomyRules.swift WWIIHexV0/SpriteKit/MapDisplayAdapter.swift WWIIHexV0/UI/RegionInspectorView.swift WWIIHexV0/UI/DiplomacyPanelView.swift WWIIHexV0/UI/RootGameView.swift WWIIHexV0/Agents/AgentContexts.swift WWIIHexV0/Agents/ZoneCommanderAgent.swift`：通过。

未跑 Xcode build/test、XCTest、模拟器、Probe、Smoke、Stage Regression、Dynamic Theater Regression、Full；原因是当前任务未授权本机重测试，项目规范默认只跑轻量检查并交给云端 Actions 做重验证。

## 7. 云端验证

- 提交：`5879d6b17a615bd9cfcfd90755aed7a294e28061`。
- GitHub Actions run：`28717840389`，attempt 1，workflow `WWIIHexV0 CI Results`，结论 success。
- Artifact：`wwiihexv0-ci-cloud-process-main-5879d6b-run28717840389-attempt1`，已下载到 `/private/tmp/wwiihexv0-v44-governance-review-28717840389/`。
- manifest 核对：`branch=main`、`commitSha=5879d6b17a615bd9cfcfd90755aed7a294e28061`、`shortSha=5879d6b`、`runId=28717840389`、`runAttempt=1`、`staticChecksOutcome=success`、`buildOutcome=success`、`testOutcome=skipped`。
- `junit.xml`：2 tests，0 failures。
- `static-checks.log`：`git diff --check`、`plutil`、`xmllint`、`jq` 均通过。
- `xcodebuild.log`：`WWIIHexV0Mac` Debug build 以 `** BUILD SUCCEEDED **` 结束。
